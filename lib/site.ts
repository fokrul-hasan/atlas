// Update this once you're happy with which domain is your "real" one
// (or once you connect a custom domain). Falls back to your Netlify URL.
export const SITE_URL = process.env.NEXT_PUBLIC_SITE_URL || "https://fokrulh.netlify.app";
